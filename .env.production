# Production Environment Configuration
# Copy this file to .env and update with your production values

# Django Settings
DEBUG=False
SECRET_KEY=LudVWRWQsEpAgZ4PMtMAjQoxpLU7aIOj-JVpfa7mb7CVl3Uwog-prw-R91qMgI_JmI0

# Allowed Hosts (your domain)
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Database Credentials (use strong passwords in production!)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=project_management_db
DB_USER=postgres
DB_PASSWORD=your-secure-db-password-here

# Database Host (can be 'db' if using Docker, or your cloud DB URL)
DB_HOST=db
DB_PORT=5432

# Redis Configuration
REDIS_URL=redis://redis:6379/0
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_CACHE_URL=redis://redis:6379/1
CELERY_BROKER_URL=redis://redis:6379/0

# Elasticsearch (optional)
ELASTICSEARCH_URL=http://elasticsearch:9200

# CORS - Allow your production domain
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# WebSocket - Use secure WebSocket protocol in production
VITE_API_BASE_URL=https://yourdomain.com/api
VITE_WS_URL=wss://yourdomain.com

# Frontend App Name
VITE_APP_NAME=Project Management Dashboard

# Security Settings (MUST be True in production)
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
CSRF_COOKIE_HTTPONLY=True

# Email Configuration (SMTP for password resets and notifications)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-specific-password
EMAIL_USE_TLS=True

# Error Tracking (Sentry - optional)
SENTRY_DSN=

# Logging (optional)
LOG_LEVEL=INFO
